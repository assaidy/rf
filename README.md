# Rename file by replacing regex patterns.

```
Usage:
fr <FILE_PATH> <REGEX> <REPLACEMENT>

    FILE_PATH    the file path you want rename
    REGEX        search pattern
    REPLACEMENT  string replacement for regex matches
```
